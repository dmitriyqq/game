#pragma once

namespace Engine{
    namespace Input{
        enum class Key{ UP, DOWN, LEFT, RIGHT, ACCEPT, ACTION, EXIT, MORE, LESS };

        enum class SpecialKey{SHIFT, CONTROL, ALT, SUPER};
    }
}